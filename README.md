Template used: https://startbootstrap.com/theme/sb-admin-2

Add cronJob.php file to your server's cron job for a DB feature

You will need to install all the required python packages for the script. You can do this by running `pip install -r requirements.txt` in the directory where you